<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bangunruang">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BangunRuang">
        <activity android:name=".LingkaranActivity"></activity>
        <activity android:name=".TrapesiumActivity" />
        <activity android:name=".LayangLayangActivity" />
        <activity android:name=".BelahKetupatActivity" />
        <activity android:name=".SegitigaActivity" />
        <activity android:name=".JajarGjgActivity" />
        <activity android:name=".PersegiPjgActivity" />
        <activity android:name=".PersegiActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>